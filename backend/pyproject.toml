[project]
name = "insightops-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.115.0",
]

[project.optional-dependencies]
test = [
    "httpx==0.27.2",
    "pytest==8.3.3",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
